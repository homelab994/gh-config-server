spring:
  application.name: gh-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/homelab994/gh-config-repo
          default-label: develop
          search-paths:
            - "*"

server:
  port: 9090